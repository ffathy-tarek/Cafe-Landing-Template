/**
 * Social media links for the business.
 * All fields are optional — if a URL is not provided,
 * the corresponding icon/link should be omitted from the UI.
 */
export type SocialLinks = {
  /** Instagram profile URL */
  instagram?: string;

  /** Facebook page URL */
  facebook?: string;
};
